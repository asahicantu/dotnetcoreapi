-- SQLite
-- To use from commandline use sqlite3 newspaper.db
-- Later can use .tables to show all db tables
-- Use .schema table [articles is a suitable table] to show the table structure
select * from articles;